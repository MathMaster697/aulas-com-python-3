# Importa a biblioteca pandas comumente apelidada como pd
import pandas as pd

# Cria um DataFrame vazio usando o pandas
df = pd.DataFrame()

# Imprime o DataFrame vazio
print(df)

# import pandas as pd: Importa a biblioteca pandas e a apelida como pd para facilitar a referência posterior no código.

# df = pd.DataFrame(): Cria um DataFrame vazio usando a função DataFrame do pandas e atribui à variável df.

# print(df): Imprime o DataFrame vazio. O DataFrame é uma estrutura de dados tabular do pandas, semelhante a uma tabela em um banco de dados ou uma planilha do Excel. Neste caso, como nenhum dado foi fornecido, o DataFrame está vazio.

# Um DataFrame é uma estrutura de dados tabular do pandas, semelhante a uma tabela em um banco de dados ou uma planilha do Excel.
# Ele é composto por linhas e colunas, onde cada coluna pode conter diferentes tipos de dados.
# Neste caso, como nenhum dado foi fornecido ao criar o DataFrame, ele está vazio.